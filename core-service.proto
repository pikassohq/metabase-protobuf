syntax = "proto3";

package CoreService;

import "transaction.proto";
import "project.proto";

service CoreService {
  rpc UpdateTransactionById (transaction.Request) returns (transaction.Transaction) {}
  rpc UpdateProjectById (project.Request) returns (project.Project) {}
}
